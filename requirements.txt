# HTTP
requests>=2.31.0

# PostgreSQL (Neon). psycopg 3 with prepare_threshold=None works through Neon's pooler.
psycopg[binary]>=3.1.0

# AES (CryptoJS-compatible) payloads for the applay platform (X Supermercados, Barbosa)
pycryptodome>=3.19.0

# OPTIONAL - only used as a last-resort fallback to rediscover rotating tokens
# (Rossi VipCommerce bearer, applay Next.js action ids). Not needed for normal runs.
# playwright>=1.40.0   (then: playwright install chromium)

# OPTIONAL - Chrome TLS impersonation for hosts with aggressive bot detection (Higas/ibecom).
# The Higas scraper uses it automatically when importable; plain requests otherwise.
# curl_cffi>=0.7
